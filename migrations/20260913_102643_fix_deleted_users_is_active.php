<?php
/**
 * ═══════════════════════════════════════════════════════════════════
 *  مهاجرت: هماهنگ‌سازی is_active با is_deleted برای کاربران حذف‌شده
 *  تاریخ: ۱۴۰۵/۰۶/۲۲
 * ───────────────────────────────────────────────────────────────────
 *  چرا؟
 *    api/admin/delete-user.php تا امروز فقط is_deleted=1 می‌ذاشت و
 *    هرگز is_active را صفر نمی‌کرد. چون خیلی از پیکرها/لیست کاربران
 *    سراسر پروژه فقط is_active=1 را چک می‌کنند (نه is_deleted)، هر
 *    کاربر حذف‌شده همچنان توی دراپ‌داون‌های واگذاری کار، چت،
 *    گزارش تیم و... به پرسنل عادی نشان داده می‌شد.
 *
 *    delete-user.php و restore-user.php حالا این دو ستون را با هم
 *    هماهنگ نگه می‌دارند (کامیت همین تاریخ). این مهاجرت فقط داده‌های
 *    قدیمی از قبل ناهماهنگ را (روی همه‌ی سازمان‌ها) یک‌بار درست می‌کند.
 *
 *  نکته: این فقط داده است، نه اسکیما.
 * ═══════════════════════════════════════════════════════════════════
 */

return [

    'description' => 'هماهنگ‌سازی is_active=0 برای همه‌ی کاربران حذف‌شده (is_deleted=1) در همه‌ی سازمان‌ها',

    /**
     * اعمال تغییر
     */
    'up' => function (PDO $db) {
        $db->exec("UPDATE `users` SET `is_active` = 0 WHERE `is_deleted` = 1 AND `is_active` = 1");
    },

    /**
     * بازگرداندن تغییر
     *
     * ⚠️ این یک اصلاح داده‌ای یک‌طرفه است — قبل از این مهاجرت،
     * is_active=1 روی یک کاربر حذف‌شده خودش یک باگ بود، نه یک
     * وضعیت معتبر که بخواهیم بازش گردانیم. down() عمدا کاری نمی‌کند.
     */
    'down' => function (PDO $db) {
        // عمدا خالی — نگاه کن به توضیح بالا.
    },

];
