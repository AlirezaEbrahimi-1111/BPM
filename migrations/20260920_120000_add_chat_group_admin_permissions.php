<?php
/**
 * ═══════════════════════════════════════════════════════════════════
 *  مهاجرت: اختیارات اختصاصی برای هر مدیر گروه
 *  تاریخ: ۱۴۰۵/۰۶/۲۹
 * ───────────────────────────────────────────────────────────────────
 *  چرا؟
 *    تا امروز role='admin' یعنی «همه‌ی اختیارات مدیریتی» (سنجاق، افزودن/
 *    حذف عضو، تغییر عکس) — یک بسته‌ی همه‌یا‌هیچ. حالا سازنده‌ی گروه
 *    می‌تونه برای هر مدیر جداگانه مشخص کنه دقیقا کدوم اختیارات رو داره.
 *
 *  چرا NULL و نه آرایه‌ی خالی به‌عنوان پیش‌فرض؟
 *    NULL یعنی «همه‌ی اختیارات پیش‌فرض» — تا مدیرهای ازقبل‌موجود (که تا
 *    امروز همه‌کاره بودن) بدون نیاز به بک‌فیل دستی، دقیقا همون رفتار
 *    قبلی رو حفظ کنن. فقط وقتی سازنده صراحتا یک ست سفارشی برای یک
 *    مدیر خاص ثبت کنه، این ستون یک آرایه‌ی JSON (مثلا ["pin","avatar"])
 *    می‌شه و از اون به بعد دقیقا همون‌ها مجازن، نه پیش‌فرض.
 *
 *  خود سازنده (created_by) از این محدودیت مستثناست — همیشه همه‌ی
 *  اختیارات رو داره، صرف‌نظر از این ستون (چک در کد اپلیکیشن).
 * ═══════════════════════════════════════════════════════════════════
 */

return [

    'description' => 'افزودن ستون permissions (JSON، NULL=همه) به chat_participants برای اختیارات اختصاصی هر مدیر',

    'up' => function (PDO $db) {
        $stmt = $db->query("SHOW COLUMNS FROM `chat_participants` LIKE 'permissions'");
        if (!$stmt->fetch()) {
            $db->exec("
                ALTER TABLE `chat_participants`
                ADD COLUMN `permissions` JSON NULL DEFAULT NULL AFTER `role`
            ");
        }
    },

    'down' => function (PDO $db) {
        $stmt = $db->query("SHOW COLUMNS FROM `chat_participants` LIKE 'permissions'");
        if ($stmt->fetch()) {
            $db->exec("ALTER TABLE `chat_participants` DROP COLUMN `permissions`");
        }
    },

];
