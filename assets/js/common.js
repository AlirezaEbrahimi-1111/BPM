/**
 * assets/js/common.js
 * توابعِ کوچکِ سراسریِ اپ که قبلاً هرکدوم جداگانه در دوجین فایل تکرار می‌شدن.
 * هدف: از این به بعد، هر صفحه‌ی جدید همینجا رو صدا بزنه به‌جایِ کپی‌کردنِ
 * دوباره‌ی همین چندخط — دقیقاً همون الگویی که باعث شد یک دور کاملِ این
 * جلسه صرفِ پیداکردنِ نقاطِ فراموش‌شده در ۱۲+ فایل بشه.
 *
 * ⚠️ نکته: خیلی از صفحات از قبل نسخه‌ی محلیِ خودشون از toFa/faNum/enTofaNumber
 * رو دارن که رفتارِ null/undefined‌شون با این‌جا فرق می‌کنه (مثلاً بعضی‌ها
 * برایِ ورودیِ خالی «—» برمی‌گردونن، بعضی‌ها «» ، بعضی‌ها «۰»). چون در
 * جاوااسکریپت تعریفِ بعدیِ یک تابعِ هم‌نام، تعریفِ قبلی رو override می‌کنه،
 * اون نسخه‌هایِ محلی عمداً دست‌نخورده باقی موندن (بعد از این فایل لود
 * می‌شن و بی‌سروصدا جایگزینش می‌کنن) — این فایل فقط برایِ صفحاتی‌ه که
 * هنوز هیچ نسخه‌ی محلی ندارن، تا اون باگِ «یادمون رفت اینجا هم اضافه کنیم»
 * دیگه پیش نیاد.
 */

// تبدیلِ اعدادِ لاتین به فارسی — برایِ هر عددی که قراره به کاربر نمایش داده بشه
function toFa(n) {
    if (n === null || n === undefined || n === '') return '';
    return String(n).replace(/\d/g, d => '۰۱۲۳۴۵۶۷۸۹'[d]);
}

// نام‌هایِ مستعارِ تاریخی — همون تابع، برایِ سازگاری با کدهایی که قبلاً
// این اسم‌ها رو صدا می‌زدن
const enTofaNumber = toFa;
const faNum = toFa;
